Examples
========

In this directory we give some examples, how `ALPBench` can be properly used. In the docs/source/notebooks directory, we provide some Jupyter notebooks that show how to use `ALPBench` in more detail.